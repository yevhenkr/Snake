using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

namespace RZ
{
    public class ___BorderLine : MonoBehaviour
    {

        //     LineRenderer line;
        //     List<Vector3> positions;
        //     float width = 0.5f;
        //     RectTransform rect;

        //     public Color LineColor = Color.gray;

        // void Start() 
        // {
        //            rect = gameObject.GetComponent<RectTransform> ();
        //            positions = new List<Vector3>();
        //            positions.Add (new Vector3 (- rect.rect.width / 2, rect.rect.height / 2, 0));
        //            positions.Add (new Vector3 (rect.rect.width / 2, rect.rect.height / 2, 0));
        //            positions.Add (new Vector3 (rect.rect.width / 2, - rect.rect.height / 2, 0));
        //            positions.Add (new Vector3 (- rect.rect.width / 2, - rect.rect.height / 2, 0));
        //            line = gameObject.AddComponent<LineRenderer>();
        //            line.material = AssetDatabase.GetBuiltinExtraResource<Material>("Sprites-Default.mat");
        //            line.startWidth = width;
        //            line.endWidth = width;
        //            // line.SetColors (LineColor, Color.cyan);
        //            line.positionCount = positions.Count;
        //            line.SetPositions(positions.ToArray());
        //            line.useWorldSpace = false;
        //            line.shadowCastingMode = UnityEngine.Rendering.ShadowCastingMode.Off;
        //            line.receiveShadows = false;
        //            line.textureMode = LineTextureMode.Tile;
        //            line.loop = true;
        //            line.numCornerVertices = 8;
        // }
    }
}
